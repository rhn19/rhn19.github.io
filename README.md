# rhn19.github.io

Personal portfolio website for Rohan Bhide.

Built with vanilla HTML, CSS, and JavaScript. Zero dependencies in production.

## Development

```bash
npm install
npm start        # local dev server on :3000
npm test         # run validation suite
```

## Deployment

Automatically deployed via GitHub Pages from the `main` branch.
